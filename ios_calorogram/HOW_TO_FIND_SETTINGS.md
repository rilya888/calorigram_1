# Как найти настройки в Xcode

## 1. Настройка Signing & Capabilities

### Шаг 1: Откройте проект
- Дважды кликните на `Calorigram.xcodeproj` в Finder
- Или выполните: `open Calorigram.xcodeproj`

### Шаг 2: Выберите проект в навигаторе
1. В левой панели Xcode (навигатор) найдите самый верхний элемент - это **синий иконка проекта "Calorigram"**
2. Кликните на него **один раз** (не двойной клик)

### Шаг 3: Выберите target
1. В центральной панели вы увидите список targets
2. Под заголовком **TARGETS** найдите **Calorigram** (не CalorigramTests)
3. Кликните на **Calorigram** один раз

### Шаг 4: Откройте вкладку Signing & Capabilities
1. В верхней части центральной панели вы увидите вкладки:
   - **General**
   - **Signing & Capabilities** ← **ВОТ ОНА!**
   - **Resource Tags**
   - **Info**
   - **Build Settings**
   - и т.д.
2. Кликните на вкладку **"Signing & Capabilities"**

### Шаг 5: Настройте Team
1. В разделе **Signing** найдите выпадающий список **Team**
2. Выберите вашу команду разработчика (или оставьте "None" для локальной разработки)
3. Если команды нет, Xcode предложит создать её

### Шаг 6: Проверьте Capabilities
1. Прокрутите вниз до раздела **Capabilities**
2. Убедитесь, что **Sign in with Apple** добавлена (должна быть автоматически из entitlements)
3. Если её нет, нажмите **"+ Capability"** и добавьте

---

## 2. Проверка, что все файлы добавлены в проект

### Способ 1: Визуальная проверка в Xcode

1. В левой панели (навигатор) найдите папку **Calorigram** (желтая папка)
2. Разверните её (кликните на стрелку слева)
3. Вы должны увидеть структуру:
   ```
   Calorigram
   ├── App
   │   ├── CalorigramApp.swift
   │   └── ContentView.swift
   ├── Models
   │   ├── Meal.swift
   │   ├── Statistics.swift
   │   ├── Subscription.swift
   │   └── User.swift
   ├── Services
   │   ├── APIService.swift
   │   ├── AppleAuthService.swift
   │   ├── AuthService.swift
   │   ├── KeychainService.swift
   │   └── NotificationService.swift
   ├── ViewModels
   │   ├── AddMealViewModel.swift
   │   ├── AnalysisViewModel.swift
   │   ├── AuthViewModel.swift
   │   ├── DiaryViewModel.swift
   │   ├── HomeViewModel.swift
   │   ├── ProfileViewModel.swift
   │   └── StatsViewModel.swift
   ├── Views
   │   ├── Analysis
   │   ├── Auth
   │   ├── Diary
   │   ├── Home
   │   ├── Modals
   │   ├── Onboarding
   │   ├── Profile
   │   ├── Stats
   │   └── MainTabView.swift
   └── Utils
       ├── Constants.swift
       └── Extensions.swift
   ```

4. Если каких-то файлов нет:
   - Правой кнопкой на папку **Calorigram** в навигаторе
   - Выберите **"Add Files to 'Calorigram'..."**
   - Выберите недостающие файлы/папки
   - Убедитесь, что отмечено:
     - ✅ **Copy items if needed**
     - ✅ **Create groups** (НЕ folder references!)
     - ✅ **Add to targets: Calorigram**

### Способ 2: Проверка через командную строку

Выполните команду для проверки количества файлов:

```bash
cd /Users/mac/ios_calorogram
echo "Файлов Swift в проекте:"
find Calorigram -name "*.swift" | wc -l
echo ""
echo "Список всех Swift файлов:"
find Calorigram -name "*.swift" | sort
```

---

## 3. Альтернативный способ проверки файлов

### Через Build Phases

1. Выберите проект в навигаторе (синяя иконка)
2. Выберите target **Calorigram**
3. Перейдите на вкладку **Build Phases**
4. Разверните **Compile Sources**
5. Там должны быть все `.swift` файлы проекта

Если файлов нет в **Compile Sources**, добавьте их:
- Нажмите **"+"** внизу списка
- Выберите недостающие файлы

---

## 4. Если файлы не видны в навигаторе

### Вариант 1: Обновить проект через xcodegen

```bash
cd /Users/mac/ios_calorogram
xcodegen generate
```

Затем откройте проект заново.

### Вариант 2: Добавить файлы вручную

1. В Xcode: **File → Add Files to "Calorigram"...**
2. Выберите папку `Calorigram/`
3. Убедитесь, что выбрано:
   - ✅ **Create groups**
   - ✅ **Add to targets: Calorigram**
4. Нажмите **Add**

---

## Визуальная подсказка

В Xcode интерфейс выглядит так:

```
┌─────────────────────────────────────────┐
│ [📁] Calorigram          [⚙️] [▶️]      │ ← Верхняя панель
├──────────┬──────────────────────────────┤
│          │                              │
│ [📁]     │  [General] [Signing &        │ ← Вкладки
│ Calorigram│  Capabilities] [Info]...     │
│  ├─ App  │                              │
│  ├─ Models│  Signing:                   │
│  ├─ ...  │    Team: [None ▼]           │ ← Здесь выбираете Team
│          │                              │
│          │  Capabilities:               │
│          │    ✅ Sign in with Apple     │
│          │                              │
└──────────┴──────────────────────────────┘
```

---

## Быстрая проверка через терминал

Выполните эту команду для проверки структуры:

```bash
cd /Users/mac/ios_calorogram
echo "=== Структура файлов ==="
find Calorigram -type f -name "*.swift" | sort
echo ""
echo "=== Всего Swift файлов: ==="
find Calorigram -type f -name "*.swift" | wc -l
```

