# Быстрое исправление

## Проблема
При запуске сразу показывается WelcomeScreen вместо AuthView.

## Причина
В Keychain остался токен от предыдущих тестов.

## Решение

### Шаг 1: Удалите приложение с симулятора
```
В симуляторе:
1. Долгое нажатие на иконку Calorigram
2. Нажмите "-" (удалить)
3. Подтвердите удаление
```

### Шаг 2: Пересоберите в Xcode
```
1. Product → Clean Build Folder (⇧⌘K)
2. Product → Build (⌘B)
3. Product → Run (⌘R)
```

### Шаг 3: Проверьте
```
Теперь должен показаться AuthView с выбором способа регистрации ✅
```

## Что исправлено в коде

1. **AuthViewModel.checkAuthStatus()** - теперь проверяет валидность токена через API
2. **AuthViewModel.loadCurrentUser()** - автоматически очищает невалидные токены
3. **ContentView** - показывает загрузку, пока данные пользователя загружаются

## Теперь работает так:

```
Запуск
  ↓
Есть токен? 
  ├─ НЕТ → AuthView ✅
  └─ ДА → Проверка через API
           ├─ Валидный + профиль заполнен → MainTabView ✅
           ├─ Валидный + профиль пустой → WelcomeScreen → Onboarding ✅
           └─ Невалидный → Очистка токенов → AuthView ✅
```

Готово! 🎉
