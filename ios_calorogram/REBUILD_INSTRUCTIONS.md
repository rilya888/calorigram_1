# 🔄 Инструкция по пересборке iOS приложения

## Зачем пересобирать?

Мы добавили подробное логирование для отладки регистрации:
- ✅ Логирование всех API запросов в `APIService.swift`
- ✅ Логирование процесса регистрации в `AuthViewModel.swift`
- ✅ Улучшенная обработка ошибок

## Как пересобрать

### В Xcode:

1. **Откройте проект:**
   ```bash
   open /Users/mac/ios_calorogram/Calorigram.xcodeproj
   ```
   Или просто откройте Xcode и выберите проект

2. **Очистите предыдущую сборку:**
   - Нажмите **⌘⇧K** (Command + Shift + K)
   - Или **Product → Clean Build Folder**

3. **Соберите проект:**
   - Нажмите **⌘B** (Command + B)
   - Или **Product → Build**
   - Дождитесь "Build Succeeded"

4. **Запустите приложение:**
   - Выберите **симулятор** (iPhone 17 Pro) вверху
   - Нажмите **⌘R** (Command + R)
   - Или кнопку ▶️

### Через терминал:

```bash
cd /Users/mac/ios_calorogram
xcodebuild clean build -project Calorigram.xcodeproj -scheme Calorigram -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
```

## Что изменилось

### 1. APIService.swift
- Добавлено логирование всех запросов (🌐 API Request)
- Добавлено логирование ответов (📥 Response)
- Добавлено логирование ошибок (❌ Error)
- Улучшена обработка retry с логированием

### 2. AuthViewModel.swift
- Добавлено логирование процесса регистрации
- Улучшена обработка ошибок с детальными сообщениями

## После пересборки

1. **Откройте консоль в Xcode:**
   - Нажмите **⌘⇧Y** (Command + Shift + Y)
   - Или **View → Debug Area → Show Debug Area**

2. **Попробуйте зарегистрироваться:**
   - Введите email, пароль, имя
   - Нажмите "Зарегистрироваться"

3. **Смотрите логи:**
   - Должны появиться строки с 🌐, 📤, 📥, ✅, ❌
   - Это поможет понять, что происходит при регистрации

## Проверка

После пересборки проверьте:

- ✅ Приложение запускается без ошибок
- ✅ Экран авторизации отображается
- ✅ Можно попробовать зарегистрироваться
- ✅ В консоли видны логи API запросов

---

**Готово! Теперь можно тестировать регистрацию с подробными логами.** 🎉

