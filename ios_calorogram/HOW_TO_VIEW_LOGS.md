# Как смотреть логи в Xcode

## 📋 Где находятся логи

### 1. Консоль (Console) - Основное место для логов

**Как открыть:**
- Нажмите **⌘⇧Y** (Command + Shift + Y)
- Или: **View → Debug Area → Show Debug Area**
- Или: Внизу Xcode нажмите на кнопку с иконкой консоли

**Что показывается:**
- Все `print()` и `NSLog()` из вашего кода
- Системные сообщения iOS
- Ошибки выполнения
- Предупреждения

**Визуальное расположение:**
```
┌─────────────────────────────────────┐
│  [Код]                              │
│                                     │
├─────────────────────────────────────┤
│  [Debug Area]                       │
│  ┌───────────────────────────────┐ │
│  │ Variables (слева)             │ │
│  │                               │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │ Console (справа) ← ЛОГИ ЗДЕСЬ│ │
│  │ print("Hello")                │ │
│  │ Error: ...                    │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

### 2. Issue Navigator - Ошибки и предупреждения

**Как открыть:**
- Нажмите **⌘4** (Command + 4)
- Или: В левой панели нажмите на иконку с восклицательным знаком ⚠️

**Что показывается:**
- Ошибки компиляции
- Предупреждения
- Проблемы с кодом

---

### 3. Report Navigator - История сборок

**Как открыть:**
- Нажмите **⌘9** (Command + 9)
- Или: В левой панели нажмите на иконку с часами 🕐

**Что показывается:**
- История всех сборок
- Логи сборки
- Время сборки
- Ошибки сборки

---

### 4. Breakpoint Navigator - Точки останова

**Как открыть:**
- Нажмите **⌘8** (Command + 8)
- Или: В левой панели нажмите на иконку с точкой останова

**Что показывается:**
- Все точки останова
- Условия для точек останова

---

## 🔍 Как использовать логи для отладки

### Добавление логов в код

```swift
// Обычный лог
print("Загрузка данных...")

// Лог с меткой
print("✅ Данные загружены успешно")

// Лог с объектом
print("User:", user)

// Лог с форматированием
print("Calories: \(calories) / \(goal)")
```

### Фильтрация логов в консоли

1. В консоли есть поле поиска (вверху справа)
2. Введите текст для фильтрации
3. Например: "Error", "API", "Auth"

### Очистка консоли

- Нажмите **⌘K** (Command + K) в консоли
- Или: Правой кнопкой в консоли → "Clear Console"

---

## 🎯 Полезные команды для логов

### В консоли Xcode:

- **⌘K** - Очистить консоль
- **⌘F** - Поиск в консоли
- **⌘⇧Y** - Показать/скрыть Debug Area

### В коде Swift:

```swift
// Разные уровни логов
print("Info: обычное сообщение")
print("⚠️ Warning: предупреждение")
print("❌ Error: ошибка")
print("✅ Success: успех")
```

---

## 📱 Логи симулятора

### Через Xcode:
- Логи симулятора автоматически показываются в консоли Xcode

### Через терминал:
```bash
# Логи симулятора
xcrun simctl spawn booted log stream

# Логи конкретного приложения
xcrun simctl spawn booted log stream --predicate 'processImagePath contains "Calorigram"'
```

---

## 🔧 Настройка логов

### В Xcode:

1. **Product → Scheme → Edit Scheme...**
2. Вкладка **"Run"**
3. Раздел **"Options"**
4. Можно настроить:
   - Console: "Use Terminal" или "Use Xcode"
   - Environment Variables
   - Arguments

---

## 💡 Советы

1. **Используйте эмодзи** для быстрого поиска:
   ```swift
   print("✅ Success")
   print("❌ Error")
   print("⚠️ Warning")
   ```

2. **Группируйте логи** по функциональности:
   ```swift
   print("[API] Request started")
   print("[API] Response received")
   ```

3. **Используйте breakpoints** для детальной отладки:
   - Кликните слева от номера строки
   - При запуске выполнение остановится
   - Можно посмотреть значения переменных

4. **Фильтруйте логи** по ключевым словам:
   - "Error" - только ошибки
   - "API" - только API запросы
   - "Auth" - только авторизация

---

## 🎯 Быстрый доступ

**Горячие клавиши:**
- **⌘⇧Y** - Показать/скрыть консоль (Debug Area)
- **⌘4** - Issue Navigator (ошибки)
- **⌘9** - Report Navigator (история сборок)
- **⌘8** - Breakpoint Navigator
- **⌘K** - Очистить консоль

---

## ✅ Проверка логов при запуске

1. Запустите приложение (⌘R)
2. Откройте консоль (⌘⇧Y)
3. Смотрите логи в реальном времени
4. Ищите ошибки (красным цветом)
5. Используйте поиск для фильтрации

---

## 📝 Примеры полезных логов

```swift
// В ViewModel
func loadData() async {
    print("🔄 [HomeViewModel] Начало загрузки данных")
    isLoading = true
    
    do {
        let data = try await apiService.fetchData()
        print("✅ [HomeViewModel] Данные загружены: \(data)")
        self.data = data
    } catch {
        print("❌ [HomeViewModel] Ошибка: \(error.localizedDescription)")
        errorMessage = error.localizedDescription
    }
    
    isLoading = false
    print("✅ [HomeViewModel] Загрузка завершена")
}

// В API Service
func request<T>(...) async throws -> T {
    print("🌐 [APIService] \(method) \(endpoint)")
    // ...
    print("✅ [APIService] Response: \(response)")
    // ...
}
```

---

## 🎉 Готово!

Теперь вы знаете, где смотреть логи в Xcode. Используйте **⌘⇧Y** для быстрого доступа к консоли!

