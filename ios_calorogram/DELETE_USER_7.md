# Как удалить пользователя ID 7 из базы данных

## Проблема
Пользователь с ID 7 создан в базе, но:
- Имя пустое (`name: ""`)
- Email NULL
- Профиль не заполнен
- Не отображается в админке бота (так как `name` пустое)

## Решение 1: Через Telegram бот админку

1. Откройте бота в Telegram
2. Отправьте `/admin`
3. Нажмите "👥 Пользователи"
4. Проверьте логи на Railway - должна быть строка:
   ```
   User 3: ID=7, telegram_id=None, email=None, name=
   ```
5. Если пользователь отображается - удалите его через кнопку "🗑️ Удалить"
6. Если НЕ отображается - используйте Решение 2

## Решение 2: Удалить через Telegram бот вручную

1. Откройте бота в Telegram
2. `/admin` → "⭐ Управление подписками"
3. Нажмите "🗑️ Удалить пользователя"
4. Введите: `7` (ID пользователя)
5. Подтвердите удаление

## Решение 3: Добавить кнопку очистки в iOS приложение

Добавьте временную кнопку в ProfileView для отладки:

```swift
Button("🗑️ Очистить токены и данные") {
    // Logout
    authViewModel.logout()
    
    // Очистить флаги
    UserDefaults.standard.removeObject(forKey: "hasCompletedOnboarding")
    UserDefaults.standard.removeObject(forKey: "hasSeenWelcome")
}
.foregroundColor(.red)
```

После нажатия:
1. Перезапустите приложение (⌘R)
2. Должен показаться AuthView
3. Зарегистрируйтесь с НОВЫМ email (не rilya888@gmail.com)

## Решение 4: Удалить приложение (самое простое)

1. В симуляторе - долгое нажатие на Calorigram
2. Нажать "-" (удалить)
3. Подтвердить
4. Запустить из Xcode (⌘R)
5. Зарегистрироваться с НОВЫМ email

## После удаления пользователя 7

Проверьте через Telegram бот:
1. `/admin` → "👥 Пользователи"
2. Должно быть только 2 пользователя (Telegram пользователи)
3. iOS приложение должно показывать AuthView при запуске

## Почему так произошло?

При тестировании регистрации был создан пользователь ID 7 с пустыми данными:
- Backend создал запись в таблице `users`
- Но имя не было заполнено
- Токены были сохранены в Keychain
- При следующем запуске приложение нашло токен и считает пользователя авторизованным

**Это нормально для тестирования!** Просто удалите пользователя и попробуйте снова.

