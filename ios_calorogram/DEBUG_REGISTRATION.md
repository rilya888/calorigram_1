# 🔍 Диагностика проблемы с регистрацией

## Что я исправил

1. ✅ Добавил подробное логирование в `APIService.swift`
2. ✅ Улучшил обработку ошибок в `AuthViewModel.swift`
3. ✅ Теперь все запросы и ответы логируются в консоль Xcode

## Как проверить, что происходит

### Шаг 1: Откройте консоль в Xcode

1. В Xcode нажмите **⌘⇧Y** (Command + Shift + Y)
2. Или выберите **View → Debug Area → Show Debug Area**

### Шаг 2: Попробуйте зарегистрироваться

1. Введите данные для регистрации:
   - Email: `test@example.com`
   - Пароль: `password123`
   - Имя: `Test User`
2. Нажмите "Зарегистрироваться"

### Шаг 3: Смотрите логи в консоли

Вы должны увидеть логи вида:

```
🌐 API Request: POST https://calorigramback-production.up.railway.app/api/auth/register
📤 Request body: {"email":"test@example.com","password":"password123","name":"Test User"}
📝 Starting registration for email: test@example.com
📥 Response: Status 200
📥 Response body: {"access_token":"...","refresh_token":"...","user":{...}}
✅ Successfully decoded response
✅ Registration successful, user ID: 123
```

Или, если есть ошибка:

```
❌ Server error 400: {"detail":"Email already registered"}
❌ Registration error: serverError(400, {"detail":"Email already registered"})
```

## Возможные проблемы и решения

### Проблема 1: "Network error" или нет ответа

**Признаки:**
- В логах нет `📥 Response:`
- Ошибка "Ошибка сети"

**Решение:**
1. Проверьте, что backend доступен:
   ```bash
   curl https://calorigramback-production.up.railway.app/api/auth/register
   ```
2. Проверьте интернет-соединение симулятора
3. Проверьте, что URL правильный в `Constants.swift`

### Проблема 2: "Server error 400"

**Признаки:**
- В логах: `❌ Server error 400: ...`

**Возможные причины:**
- Email уже зарегистрирован
- Неверный формат данных
- Валидация не прошла

**Решение:**
- Попробуйте другой email
- Проверьте формат данных в логах (`📤 Request body:`)

### Проблема 3: "Decoding error"

**Признаки:**
- В логах: `❌ Decoding error: ...`
- В логах: `❌ Response data: ...`

**Возможные причины:**
- Формат ответа от backend не соответствует модели `AuthResponse`
- Backend вернул неожиданный формат

**Решение:**
- Проверьте формат ответа в логах
- Сравните с ожидаемым форматом в `User.swift` → `AuthResponse`

### Проблема 4: "Invalid URL"

**Признаки:**
- В логах нет `🌐 API Request:`
- Ошибка "Неверный URL"

**Решение:**
- Проверьте `Constants.apiBaseURL` в `Constants.swift`
- Убедитесь, что URL правильный и начинается с `https://`

## Что делать дальше

1. **Скопируйте логи из консоли Xcode** (все строки с 🌐, 📤, 📥, ✅, ❌)
2. **Отправьте мне эти логи**, и я помогу разобраться

## Проверка backend вручную

Если хотите проверить backend напрямую:

```bash
curl -X POST https://calorigramback-production.up.railway.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123","name":"Test User"}'
```

Ожидаемый ответ:
```json
{
  "access_token": "...",
  "refresh_token": "...",
  "token_type": "bearer",
  "user": {
    "id": 123,
    "email": "test@example.com",
    "name": "Test User"
  }
}
```

---

**После того, как попробуете зарегистрироваться, скопируйте логи из консоли Xcode и отправьте мне!** 🔍

