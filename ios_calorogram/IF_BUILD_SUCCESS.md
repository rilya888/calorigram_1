# Если BUILD SUCCEEDED, но приложение не запускается

## ✅ Сборка успешна - что дальше?

Если Xcode показывает "BUILD SUCCEEDED", но при запуске ошибка, сделайте следующее:

### 1. Проверьте, что выбран СИМУЛЯТОР

**Вверху Xcode:**
- Рядом с кнопкой ▶️ должно быть: **"iPhone 17 Pro"** (или другой симулятор)
- **НЕ** должно быть: "Any iOS Device"

**Если выбрано "Any iOS Device":**
1. Кликните на "Any iOS Device"
2. Выберите **"iPhone 17 Pro"** из списка
3. Попробуйте запустить снова (⌘R)

### 2. Запустите через Xcode

1. Убедитесь, что выбран симулятор
2. Нажмите **⌘R** (Command + R) или кнопку ▶️
3. Симулятор должен открыться автоматически

### 3. Если все еще не работает - запустите через терминал

Выполните:

```bash
cd /Users/mac/ios_calorogram

# Найти .app файл
APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData/Calorigram-*/Build/Products/Debug-iphonesimulator -name "Calorigram.app" 2>/dev/null | head -1)

if [ -n "$APP_PATH" ]; then
    echo "✅ Найден .app: $APP_PATH"
    
    # Запустить симулятор
    open -a Simulator
    
    # Загрузить симулятор
    xcrun simctl boot "iPhone 17 Pro" 2>/dev/null || echo "Симулятор уже запущен"
    
    # Установить приложение
    echo "📱 Установка приложения..."
    xcrun simctl install booted "$APP_PATH"
    
    # Запустить приложение
    echo "▶️  Запуск приложения..."
    xcrun simctl launch booted com.calorigram.Calorigram
    
    echo "✅ Готово!"
else
    echo "❌ .app файл не найден. Пересоберите проект."
fi
```

### 4. Или используйте готовый скрипт

```bash
cd /Users/mac/ios_calorogram
./run_on_simulator.sh
```

---

## 🔍 Проверка

После запуска:
1. Симулятор должен открыться
2. Приложение должно установиться
3. Приложение должно запуститься
4. Должен появиться экран авторизации

---

## ⚠️ Важно

**Главная причина проблемы:**
- Выбран "Any iOS Device" вместо симулятора
- Для симулятора не нужен provisioning profile
- Но нужно выбрать конкретный симулятор!

---

## 📱 Визуальная подсказка

Вверху Xcode:

```
✅ ПРАВИЛЬНО:
[▶️] Calorigram > iPhone 17 Pro

❌ НЕПРАВИЛЬНО:
[▶️] Calorigram > Any iOS Device
```

---

## 🎯 Быстрое решение

1. **Выберите симулятор** вверху Xcode
2. **Нажмите ⌘R**
3. Готово!

Если не работает - используйте скрипт `./run_on_simulator.sh`

