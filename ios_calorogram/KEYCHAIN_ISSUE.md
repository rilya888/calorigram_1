# Проблема с Keychain в симуляторе

## Проблема
Даже после удаления приложения токен остается в Keychain симулятора.

## Причина
Симулятор iOS иногда не очищает Keychain при удалении приложения.

## Что было сделано
Изменен Bundle ID с `com.calorigram.app` на `com.calorigram.app.test`.

Это создаст **новый Keychain**, и старые токены не будут доступны.

## Как протестировать

1. **Пересоберите проект:**
   ```
   Product → Clean Build Folder (⇧⌘K)
   Product → Build (⌘B)
   Product → Run (⌘R)
   ```

2. **Ожидаемый результат:**
   ```
   🔍 checkAuthStatus() called
   🔍 hasToken: false
   ✅ No token found, user is not authenticated
   → AuthView показывается ✅
   ```

3. **Если все еще показывается WelcomeScreen:**
   - Сбросите симулятор: `Device → Erase All Content and Settings`
   - Запустите снова (⌘R)

## После тестирования

Когда тестирование завершено, верните оригинальный Bundle ID:
```
PRODUCT_BUNDLE_IDENTIFIER: com.calorigram.app
```

И снова выполните `xcodegen generate`.

## Альтернативное решение

Сбросить весь симулятор:
```
Device → Erase All Content and Settings...
```

Это гарантированно очистит Keychain.

## Текущий статус

✅ Bundle ID изменен на `com.calorigram.app.test`  
✅ Проект регенерирован  
⏳ Нужно пересобрать в Xcode

Пересоберите проект сейчас!

