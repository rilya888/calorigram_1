# 🔐 Переменные окружения для Railway

## ✅ ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ

Эти переменные **НЕОБХОДИМО** добавить в Railway для работы бота:

### 1. BOT_TOKEN
```
BOT_TOKEN=ваш_telegram_bot_token
```
- **Как получить:** @BotFather в Telegram → `/newbot`
- **Формат:** `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
- **Пример:** `8378784066:AAH44kisqSXLU5Cc0XzN9SIQV_kcZPG3-f0`

### 2. OPENAI_API_KEY
```
OPENAI_API_KEY=sk-ваш_openai_api_ключ
```
- **Как получить:** https://platform.openai.com/api-keys → "Create new secret key"
- **Формат:** Начинается с `sk-` или `sk-proj-`
- **Пример:** `sk-proj-abcdefghijklmnopqrstuvwxyz1234567890...`

---

## ⚙️ ОПЦИОНАЛЬНЫЕ ПЕРЕМЕННЫЕ

Эти переменные имеют значения по умолчанию и могут быть НЕ добавлены:

### 3. BASE_URL (по умолчанию: https://api.openai.com/v1/)
```
BASE_URL=https://api.openai.com/v1/
```

### 4. OPENAI_MODEL (по умолчанию: gpt-4o-mini)
```
OPENAI_MODEL=gpt-4o-mini
```

### 5. OPENAI_VISION_MODEL (по умолчанию: gpt-4o-mini)
```
OPENAI_VISION_MODEL=gpt-4o-mini
```

### 6. OPENAI_WHISPER_MODEL (по умолчанию: whisper-1)
```
OPENAI_WHISPER_MODEL=whisper-1
```

### 7. DATABASE_PATH (по умолчанию: users.db)
```
DATABASE_PATH=users.db
```

### 8. TEST_MODE (по умолчанию: False)
```
TEST_MODE=False
```

### 9. ADMIN_IDS (по умолчанию: 160308091)
```
ADMIN_IDS=ваш_telegram_id,другой_admin_id
```
- **Как узнать свой ID:** @userinfobot в Telegram
- **Формат:** Числа через запятую без пробелов

---

## 📝 Как добавить в Railway

### Способ 1: Через веб-интерфейс

1. Откройте ваш проект на Railway
2. Перейдите во вкладку **"Variables"**
3. Нажмите **"+ New Variable"**
4. Введите название переменной (например, `BOT_TOKEN`)
5. Вставьте значение
6. Нажмите **"Add"**
7. Повторите для каждой переменной

### Способ 2: Через Railway CLI

```bash
railway variables set BOT_TOKEN="ваш_telegram_bot_token"
railway variables set OPENAI_API_KEY="sk-ваш_openai_api_ключ"
railway variables set ADMIN_IDS="ваш_telegram_id"
```

---

## ✅ Минимальная конфигурация для запуска

**Достаточно добавить только 2 переменные:**

```
BOT_TOKEN=ваш_telegram_bot_token
OPENAI_API_KEY=sk-ваш_openai_api_ключ
```

Все остальное будет использовать значения по умолчанию.

---

## 🧪 Рекомендуемая конфигурация

Для полного контроля рекомендуем добавить:

```
BOT_TOKEN=ваш_telegram_bot_token
OPENAI_API_KEY=sk-ваш_openai_api_ключ
ADMIN_IDS=ваш_telegram_id
TEST_MODE=False
```

---

## 🔍 Проверка переменных

После добавления переменных:

1. **В Railway:** Variables → убедитесь что все переменные отображаются
2. **В логах:** Deployments → View Logs → ищите `Bot configuration completed`
3. **В Telegram:** Отправьте боту `/start`

---

## ⚠️ Важные замечания

### Безопасность:
- ❌ **НЕ** коммитьте .env файл в git
- ✅ Используйте Variables в Railway
- ✅ API ключи должны быть секретными

### API лимиты:
- OpenAI API имеет лимиты запросов
- Проверьте баланс: https://platform.openai.com/account/usage
- Установите лимиты: https://platform.openai.com/account/limits

### Стоимость:
- gpt-4o-mini: ~$0.15/$0.60 за 1M токенов
- Whisper: ~$0.006 за минуту
- Примерно: $5-10 в день на 1000 активных пользователей

---

## 🆘 Помощь

Если возникли проблемы:
1. Проверьте логи в Railway
2. Убедитесь что все переменные добавлены
3. Проверьте формат переменных (нет лишних пробелов)
4. Перезапустите деплой

Готово! 🎉

