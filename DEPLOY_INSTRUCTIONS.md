# 🚀 Инструкция по деплою на Railway

## ✅ Что уже готово:

- ✅ Git репозиторий инициализирован
- ✅ Все файлы закоммичены (2 коммита)
- ✅ .gitignore настроен
- ✅ Procfile готов
- ✅ railway.json готов
- ✅ runtime.txt обновлен (Python 3.12)
- ✅ requirements.txt актуален
- ✅ Код переведен на OpenAI API

---

## 📦 ВАРИАНТ 1: Через GitHub веб-интерфейс (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Заполните:
   - **Repository name:** `calorigram-openai-bot`
   - **Description:** `Telegram bot for calorie tracking using OpenAI GPT-4o-mini`
   - **Visibility:** Private (рекомендуется)
   - ❌ **НЕ ставьте галочки** на README, .gitignore, license
3. Нажмите **"Create repository"**

### Шаг 2: Запушьте код

После создания репозитория скопируйте URL и выполните:

```bash
cd /Users/mac/add_meal_multi_move_to_OpenAi

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/calorigram-openai-bot.git

# Запушьте код
git push -u origin main
```

**Пример с вашим username:**
```bash
# Если ваш username - rilya888
git remote add origin https://github.com/rilya888/calorigram-openai-bot.git
git push -u origin main
```

---

## 📦 ВАРИАНТ 2: Через GitHub CLI

```bash
cd /Users/mac/add_meal_multi_move_to_OpenAi

# Авторизуйтесь (если еще не авторизованы)
gh auth login

# Создайте и запушьте репозиторий одной командой
gh repo create calorigram-openai-bot --private --source=. --remote=origin --push
```

---

## ☁️ Шаг 3: Деплой на Railway

### 3.1 Подключите GitHub

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Найдите и выберите `calorigram-openai-bot`

### 3.2 Добавьте переменные окружения

Перейдите во вкладку **"Variables"** и добавьте:

#### ✅ ОБЯЗАТЕЛЬНЫЕ (2 переменные):

```
BOT_TOKEN=ваш_telegram_bot_token
OPENAI_API_KEY=sk-ваш_openai_api_ключ
```

#### ⚙️ РЕКОМЕНДУЕМЫЕ (1 переменная):

```
ADMIN_IDS=ваш_telegram_id
```

#### 🔧 ОПЦИОНАЛЬНЫЕ (если хотите кастомизировать):

```
BASE_URL=https://api.openai.com/v1/
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini
OPENAI_WHISPER_MODEL=whisper-1
DATABASE_PATH=users.db
TEST_MODE=False
```

### 3.3 Запустите деплой

Railway автоматически начнет деплой после:
- Подключения репозитория
- Добавления переменных

**Время деплоя:** 2-3 минуты

### 3.4 Проверьте работу

1. Откройте **Deployments** → последний деплой
2. Нажмите **"View Logs"**
3. Должно появиться:
   ```
   ✅ Bot configuration completed
   ✅ Bot commands menu configured
   ✅ Scheduler started successfully
   ```

4. Откройте бота в Telegram и отправьте `/start`

---

## 📋 Краткая памятка по переменным

### Что добавить в Railway Variables:

| Переменная | Обязательно | Значение по умолчанию | Где взять |
|------------|-------------|----------------------|-----------|
| `BOT_TOKEN` | ✅ ДА | - | @BotFather |
| `OPENAI_API_KEY` | ✅ ДА | - | platform.openai.com |
| `ADMIN_IDS` | 🟡 Рекомендуется | 160308091 | @userinfobot |
| `BASE_URL` | ❌ НЕТ | https://api.openai.com/v1/ | - |
| `OPENAI_MODEL` | ❌ НЕТ | gpt-4o-mini | - |
| `OPENAI_VISION_MODEL` | ❌ НЕТ | gpt-4o-mini | - |
| `OPENAI_WHISPER_MODEL` | ❌ НЕТ | whisper-1 | - |
| `DATABASE_PATH` | ❌ НЕТ | users.db | - |
| `TEST_MODE` | ❌ НЕТ | False | - |

---

## 🎯 Что делать дальше

### 1. Создайте GitHub репозиторий (Вариант 1 или 2 выше)
### 2. Задеплойте на Railway (Шаг 3)
### 3. Добавьте переменные (таблица выше)
### 4. Проверьте работу бота

---

## 💡 Полезные команды

### Git:
```bash
# Посмотреть статус
git status

# Посмотреть коммиты
git log --oneline

# Добавить изменения
git add .
git commit -m "Update"
git push
```

### Railway CLI:
```bash
# Установить
npm install -g @railway/cli

# Войти
railway login

# Посмотреть логи
railway logs

# Список переменных
railway variables
```

---

## 🆘 Решение проблем

### Проблема: Git push не работает
```bash
# Проверьте remote
git remote -v

# Если пусто - добавьте
git remote add origin https://github.com/YOUR_USERNAME/calorigram-openai-bot.git

# Попробуйте снова
git push -u origin main
```

### Проблема: Railway не видит репозиторий
- Убедитесь что репозиторий публичный или Railway имеет доступ
- Переподключите GitHub в настройках Railway

### Проблема: Бот не отвечает после деплоя
1. Проверьте логи в Railway
2. Убедитесь что переменные добавлены правильно
3. Проверьте баланс OpenAI API
4. Перезапустите деплой

Готово! 🚀

