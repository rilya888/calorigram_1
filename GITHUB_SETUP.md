# 🔧 Настройка GitHub и Railway

## 📦 Шаг 1: Создание GitHub репозитория

### Вариант А: Через веб-интерфейс

1. Зайдите на [github.com](https://github.com)
2. Нажмите "+" → "New repository"
3. Заполните:
   - **Repository name:** `calorigram-openai-bot`
   - **Description:** "Telegram bot for calorie tracking using OpenAI GPT-4o-mini"
   - **Visibility:** Private (рекомендуется) или Public
4. **НЕ** инициализируйте с README, .gitignore, license
5. Нажмите "Create repository"

### Вариант Б: Через GitHub CLI

```bash
gh repo create calorigram-openai-bot --private --source=. --remote=origin --push
```

## 🔗 Шаг 2: Подключение репозитория

После создания репозитория на GitHub выполните:

```bash
cd /Users/mac/add_meal_multi_move_to_OpenAi

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/calorigram-openai-bot.git

# Или используйте SSH:
# git remote add origin git@github.com:YOUR_USERNAME/calorigram-openai-bot.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Запушьте код
git push -u origin main
```

## ☁️ Шаг 3: Деплой на Railway

### 3.1 Создание проекта

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите репозиторий `calorigram-openai-bot`

### 3.2 Добавление переменных окружения

В разделе **Variables** добавьте:

#### ✅ ОБЯЗАТЕЛЬНЫЕ переменные:

```
BOT_TOKEN=ваш_telegram_bot_token
OPENAI_API_KEY=sk-ваш_openai_api_ключ
```

#### ⚙️ ОПЦИОНАЛЬНЫЕ переменные (можно не добавлять, есть дефолты):

```
BASE_URL=https://api.openai.com/v1/
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini
OPENAI_WHISPER_MODEL=whisper-1
DATABASE_PATH=users.db
TEST_MODE=False
ADMIN_IDS=ваш_telegram_id
```

### 3.3 Деплой

1. Railway автоматически начнет деплой после подключения
2. Подождите 2-3 минуты
3. Проверьте логи: вкладка "Deployments" → последний деплой → "View Logs"
4. Должно появиться: `Bot commands menu configured`

### 3.4 Проверка работы

Откройте вашего бота в Telegram и отправьте:
- `/start` - запуск бота
- Фото еды - для анализа через Vision API
- Текст "яблоко 150г" - для текстового анализа
- Голосовое сообщение - для Whisper API

## 🔍 Проверка статуса

### В Railway:
```
Logs → View Logs → Ищите:
✅ "Bot configuration completed"
✅ "Bot commands menu configured"
✅ "Scheduler started successfully"
```

### В Telegram:
```bash
# Отправьте боту /start
# Должно появиться главное меню
```

## ⚠️ Решение проблем

### "OPENAI_API_KEY не найден"
- Проверьте Variables в Railway
- Убедитесь что нет пробелов в названии переменной
- Перезапустите деплой

### "Bot not responding"
- Проверьте логи в Railway
- Убедитесь что BOT_TOKEN правильный
- Проверьте баланс OpenAI API

### "Database error"
- Railway автоматически создаст users.db
- При первом запуске база создастся автоматически

## 💡 Полезные команды

```bash
# Проверить статус git
git status

# Посмотреть коммиты
git log --oneline

# Добавить изменения
git add .
git commit -m "Update: описание изменений"
git push

# Проверить remote
git remote -v
```

## 📊 Мониторинг

После деплоя на Railway вы можете:
- Смотреть логи в реальном времени
- Отслеживать использование ресурсов
- Настроить алерты при ошибках

## 🎯 Следующие шаги

1. ✅ Создайте репозиторий на GitHub
2. ✅ Запушьте код
3. ✅ Задеплойте на Railway
4. ✅ Добавьте переменные окружения
5. ✅ Протестируйте бота

Готово! 🚀

